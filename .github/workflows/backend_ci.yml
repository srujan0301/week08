name: Backend CI - Test, Build and Push Images to ACR

on:
  workflow_dispatch: {}
  push:
    branches: [ main ]
    paths:
      - "backend/**"
      - ".github/workflows/backend_ci.yml"

env:
  # Must match your GitHub secrets (you already created these)
  ACR_NAME: ${{ secrets.ACR_NAME }}                         # e.g., sit722wk8acr123
  ACR_LOGIN_SERVER: ${{ secrets.ACR_LOGIN_SERVER }}         # e.g., sit722wk8acr123.azurecr.io
  IMAGE_TAG: ${{ github.sha }}-${{ github.run_id }}

jobs:
  test_and_lint_backends:
    runs-on: ubuntu-latest

    services:
      product_db:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: products
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports: ["5432:5432"]

      order_db:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: orders
        options: >-
          --health-cmd "pg_isready -U postgres"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports: ["5433:5432"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          for req in backend/*/requirements.txt; do
            echo "Installing $req"
            pip install -r "$req"
          done
          pip install pytest httpx

      - name: Run product_service tests
        working-directory: backend/product_service
        env:
          POSTGRES_HOST: localhost
          POSTGRES_PORT: 5432
          POSTGRES_DB: products
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        run: pytest -q

      - name: Run order_service tests
        working-directory: backend/order_service
        env:
          POSTGRES_HOST: localhost
          POSTGRES_PORT: 5433
          POSTGRES_DB: orders
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        run: pytest -q

  build_and_push_images:
    runs-on: ubuntu-latest
    needs: test_and_lint_backends

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Azure login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Ensure Buildx
        uses: docker/setup-buildx-action@v3

      # IMPORTANT: az acr login expects the **registry name**, not the login server
      - name: Login to ACR
        run: az acr login --name "${{ env.ACR_NAME }}"

      # Use **hyphen** repo names so they match your k8s manifests (product-service/order-service)
      - name: Build & Push product-service
        run: |
          docker build -t "${{ env.ACR_LOGIN_SERVER }}/product-service:latest" backend/product_service
          docker push "${{ env.ACR_LOGIN_SERVER }}/product-service:latest"

      - name: Build & Push order-service
        run: |
          docker build -t "${{ env.ACR_LOGIN_SERVER }}/order-service:latest" backend/order_service
          docker push "${{ env.ACR_LOGIN_SERVER }}/order-service:latest"

      - name: Logout Azure
        if: always()
        run: az logout
